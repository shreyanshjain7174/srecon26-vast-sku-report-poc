---
phase: 01-safety-foundation-and-local-evidence
plan: 02
type: tdd
wave: 2
depends_on: [01-01]
files_modified:
  - src/srecon26_poc/budget.py
  - src/srecon26_poc/contracts.py
  - src/srecon26_poc/provider.py
  - tests/unit/test_budget.py
  - tests/unit/test_contracts.py
  - tests/fixtures/offers/rtx3090.json
  - tests/fixtures/instances/rtx3090-running.json
autonomous: true
requirements: [BUDG-01, BUDG-02, BUDG-03, BUDG-04, BUDG-05, CTRL-03, ERR-03]
must_haves:
  truths:
    - Reservations cannot exceed the $5.00 project cap or the approved per-category caps.
    - Restarted ledger state retains open reservations and recomputes Decimal headroom without float conversion.
    - A provider create ambiguity can be reconciled by a unique label but never retried.
    - A ledger persistence failure halts the lifecycle instead of permitting a create.
  artifacts:
    - path: src/srecon26_poc/budget.py
      provides: Decimal-only persistent exposure ledger and typed budget failures.
    - path: src/srecon26_poc/contracts.py
      provides: Versioned offer/instance ownership contracts and deterministic fault classification.
    - path: src/srecon26_poc/provider.py
      provides: Read-only/provider side-effect protocol with single-create and exact-destroy contracts.
  key_links:
    - from: src/srecon26_poc/budget.py
      to: src/srecon26_poc/types.py
      via: Run identity and structured halt event contracts
      pattern: RunIdentity|HALT|BUDGET_WRITE_FAIL
    - from: src/srecon26_poc/provider.py
      to: src/srecon26_poc/contracts.py
      via: Offer and instance contract protocol signatures
      pattern: OfferContract|InstanceContract
---

<objective>
Implement and prove the strict exposure ledger plus the provider ownership contracts that make a duplicate or mis-owned paid create impossible by design.

Purpose: Establish cost and provider contract gates before lifecycle orchestration is introduced.
Output: Decimal ledger, provider/offer contracts, deterministic fault classification, fixtures, and focused tests.
</objective>

<execution_context>
@docs/superpowers/specs/2026-09-23-srecon26-llm-hpa-poc-design.md
@docs/superpowers/plans/2026-09-23-poc-foundation.md
@.planning/phases/01-safety-foundation-and-local-evidence/01-CONTEXT.md
@.planning/phases/01-safety-foundation-and-local-evidence/01-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md

<interfaces>
Consume `RunIdentity` and `FaultClass` from `src/srecon26_poc/types.py`. Export `ExposureLedger.reserve`, `ExposureLedger.commit_actual`, a read-only Decimal headroom query, `OfferContract`, `InstanceContract`, `classify_fault`, and a `Provider` protocol with `account_snapshot`, `list_instances`, `get_offer`, `create_once`, `get_instance`, and `destroy_exact`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Decimal exposure accounting and provider contracts</name>
  <files>src/srecon26_poc/budget.py, src/srecon26_poc/contracts.py, src/srecon26_poc/provider.py, tests/unit/test_budget.py, tests/unit/test_contracts.py, tests/fixtures/offers/rtx3090.json, tests/fixtures/instances/rtx3090-running.json</files>
  <behavior>
    - Cumulative reservations of `$2.00` and `$3.01` fail at the second reservation; exactly `$5.00` is permitted only when category limits also hold.
    - JSON deserialization and serialization preserve `Decimal` values without passing through `float`.
    - Open reservations survive reopen; actual spend cannot release remaining reservation until an attached three-read absence proof exists.
    - Each run category enforces its $1.00, $1.00, or $3.00 allocation cap.
    - Contract mismatches for GPU identity, count, memory, compute capability, machine, price, or label classify as `PROVIDER_FAULT_CONFIRMED`; controller/network/model-download causes classify as `DIAGNOSIS_UNRESOLVED`.
    - `create_once` is documented and typed as non-retryable on ambiguity; reconciliation uses a unique label and exact ownership data.
    - A failed atomic ledger write raises a fatal typed condition that later controller code records as `BUDGET_WRITE_FAIL`.
  </behavior>
  <action>Follow D-01 and D-04. Write the failing budget, persistence, category-cap, JSON-boundary, classification, and no-retry tests first. Use `Decimal(str(value))` plus `json.loads(..., parse_float=Decimal)` for budget values, atomic replacement with durable sync, and an OS-level lock around read-check-write. Do not import a third-party money, ORM, or provider SDK library; do not implement a live Vast adapter in this phase.</action>
  <verify>
    <automated>python -m pytest -q tests/unit/test_budget.py tests/unit/test_contracts.py &amp;&amp; git diff --check</automated>
  </verify>
  <done>Focused tests prove exact Decimal caps and restart safety, robust provider-fault classification, no float conversion on the budget path, and a provider protocol that cannot express a retry loop for create.</done>
</task>

</tasks>

<verification>
Run focused ledger and contract tests, then `git diff --check`, before integrating lifecycle orchestration.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Controller to ledger file | Concurrent or restarted control paths could otherwise overspend. |
| Provider response to contracts | Marketplace data and create outcomes are untrusted until identity and ownership fields match. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-03 | Tampering | budget.py | mitigate | Decimal-only parsing, lock-protected atomic writes, durable state, cap and category tests. |
| T-01-04 | Spoofing | contracts.py | mitigate | Match exact offer fields, instance ID, machine, price, label, and later nonce before accepting ownership. |
| T-01-05 | Denial of service | provider.py | mitigate | Ambiguous create is reconciled by label and halted, not retried. |
| T-01-SC | Tampering | Python dependency setup | accept | This plan installs no package; provider interaction is protocol and fixture based. |
</threat_model>

<success_criteria>
Focused tests prove exact Decimal caps and restart safety, robust provider-fault classification, no float conversion on the budget path, and a provider protocol that cannot express a retry loop for create.
</success_criteria>

<output>
Create `.planning/phases/01-safety-foundation-and-local-evidence/01-02-SUMMARY.md` when done.
</output>
