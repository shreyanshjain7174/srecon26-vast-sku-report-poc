---
phase: 02-live-gpu-canary-and-paired-comparison
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/srecon26_poc/desktop_report.py
  - browser/report-fixture/index.html
  - browser/report-fixture/app.js
  - scripts/preflight_report.py
  - tests/unit/test_desktop_report.py
  - tests/e2e/test_report_fixture.py
  - docs/runbooks/provider-reporting.md
autonomous: true
requirements: [GPU-01, GPU-07]
must_haves:
  truths:
    - Report automation can prove exact-target matching, bounded submission, and receipt capture using a non-submitting fixture.
    - Wrong target or unresolved diagnosis cannot click Report or authorize teardown.
  artifacts:
    - path: src/srecon26_poc/desktop_report.py
      provides: Exact-target report adapter with fixture-first execution.
    - path: browser/report-fixture/index.html
      provides: Deterministic provider-report page for non-submitting browser tests.
    - path: docs/runbooks/provider-reporting.md
      provides: Fault-only desktop reporting procedure and deadline policy.
  key_links:
    - from: src/srecon26_poc/desktop_report.py
      to: src/srecon26_poc/reporting.py
      via: Existing DesktopReportAdapter/FaultRecord/ReportReceipt contracts
      pattern: DesktopReportAdapter|FaultRecord|ReportReceipt
---

<objective>
Prove the desktop report adapter against a non-submitting exact-instance fixture before it can ever be reached from a provider-fault lifecycle.

Purpose: A report must be evidence-backed, exact-targeted, bounded, and never a remedy for controller or model failures.
Output: Fixture page, adapter, screenshot/receipt preflight, tests, and runbook.
</objective>

<context>
@.planning/phases/01-safety-foundation-and-local-evidence/01-03-SUMMARY.md
@.planning/phases/02-live-gpu-canary-and-paired-comparison/02-CONTEXT.md
<interfaces>
Consume `DesktopReportAdapter`, `FaultRecord`, and `ReportReceipt`. Export fixture mode by default plus a live mode that accepts only a frozen `PROVIDER_FAULT_CONFIRMED` bundle and exact page-visible instance ID/label.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the exact-target, non-submitting report fixture</name>
  <files>src/srecon26_poc/desktop_report.py, browser/report-fixture/index.html, browser/report-fixture/app.js, scripts/preflight_report.py, tests/unit/test_desktop_report.py, tests/e2e/test_report_fixture.py, docs/runbooks/provider-reporting.md</files>
  <behavior>
    - Matching fixture instance ID and label yields redacted before/after captures and a bounded receipt.
    - Wrong page ID, label, nonce, fault class, timeout, or stale report-start time blocks click and leaves no submitted marker.
    - Fixture preflight emits a receipt and screenshots without provider traffic or credentials.
  </behavior>
  <action>Implement per D-04 and D-06. Write failing exact-target and timeout tests first. Validate text visible in the page, the frozen fault bundle, and report-start deadline before any click. Make live mode unreachable from fixtures and require `PROVIDER_FAULT_CONFIRMED`; `DIAGNOSIS_UNRESOLVED` is a teardown-only path. Capture only redacted evidence, enforce the 60-second window, and document that a failed/timeout report yields `REPORT_UNCONFIRMED` without delaying the guard deadline.</action>
  <verify><automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests/unit/test_desktop_report.py tests/e2e/test_report_fixture.py</automated></verify>
  <done>Fixture tests prove the driver only reports the exact confirmed-fault target, captures bounded receipt evidence, and never contacts Vast.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Fault record to browser page | A stale/mismatched page could report the wrong instance. |
| Browser evidence to artifact storage | Screenshots or receipt text can leak secret/session information. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-08 | Spoofing | desktop_report.py | mitigate | Exact page ID/label/nonce and confirmed-fault checks before click. |
| T-02-09 | Information disclosure | screenshots | mitigate | Redact captures and exclude tokens/session data from receipt records. |
| T-02-10 | Denial of service | report workflow | mitigate | Bound report to 60 seconds and allow guard-directed teardown after timeout. |
</threat_model>

<success_criteria>Browser fixture tests prove exact-target reporting and timeout safety without a provider request, mutation, or credential.</success_criteria>
<output>Create `.planning/phases/02-live-gpu-canary-and-paired-comparison/02-03-SUMMARY.md` when done.</output>
