---
phase: 02-live-gpu-canary-and-paired-comparison
plan: 06
type: execute
wave: 3
depends_on: [02-05]
files_modified:
  - evidence-root-anchors/gpu-smoke-<run-id>.json
  - evidence-root-anchors/metric-path-<run-id>.json
  - artifacts/runs/<run-id>/run-manifest.json
  - artifacts/runs/<run-id>/provider/
  - artifacts/runs/<run-id>/report/
  - artifacts/runs/<run-id>/kubernetes/
  - artifacts/runs/<run-id>/metrics/
autonomous: true
requirements: [GPU-01, GPU-02, GPU-03, GPU-04, GPU-05, GPU-06, GPU-07]
user_setup:
  - service: Vast.ai
    why: "The paid stage needs an authenticated account with autobilling disabled; credential stays in the root-owned 0600 guard secret store rather than chat or repository files."
    env_vars: []
    dashboard_config:
      - task: "Keep no pre-existing instance unless it is provably owned by the same journal; do not configure a default offer, template, or permissive public ports."
  - service: independent_guard_host
    why: "A live independently reachable guard must already attest its service/timer, identity, secure secret source, and durable journal before creation."
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - No live paid stage starts unless every current safety and evidence gate passes.
    - GPU smoke and metric-path canary each end with exact teardown and three consecutive absence reads.
    - A successful metric-path bundle demonstrates all required observation hops with `provenance=real-gpu`; otherwise it records a limitation rather than claiming success.
  artifacts:
    - path: artifacts/runs/<run-id>/run-manifest.json
      provides: Versioned run identity, current contract, provenance, commands, versions, and window.
    - path: artifacts/runs/<run-id>/kubernetes/
      provides: Node/device-plugin/vLLM/HPA/events and custom-metrics evidence.
    - path: artifacts/runs/<run-id>/metrics/
      provides: Raw Prometheus plus normalized request/queue/KV/CPU timing samples.
    - path: evidence-root-anchors/gpu-smoke-<run-id>.json
      provides: Immutable root-hash/guard-receipt reference for final smoke result.
  key_links:
    - from: artifacts/runs/<run-id>/metrics/
      to: artifacts/runs/<run-id>/kubernetes/
      via: Captured vLLM to Prometheus to custom-metrics to HPA timestamps
      pattern: custom.metrics.k8s.io|Prometheus|HorizontalPodAutoscaler
    - from: artifacts/runs/<run-id>/provider/
      to: evidence-root-anchors/gpu-smoke-<run-id>.json
      via: Exact teardown and absence proof before root anchor
      pattern: absence|instance_id|label|nonce
---

<objective>
Run the paid GPU/CUDA smoke and, only after its finalization, the bounded real metric-path canary under the runner's full gate set.

Purpose: Produce genuine GPU observation evidence or an honest, checksummed limitation record while maintaining the hard exposure envelope.
Output: Finalized per-stage artifact bundles, root anchors, and absence proof; no mutable paid artifacts are committed as substitutes for anchors.
</objective>

<context>
@.planning/phases/01-safety-foundation-and-local-evidence/01-VERIFICATION.md
@.planning/phases/02-live-gpu-canary-and-paired-comparison/02-05-SUMMARY.md
@.planning/phases/02-live-gpu-canary-and-paired-comparison/02-CONTEXT.md
<interfaces>
Use only the gate-tested `scripts/run_canary.py` entry point. The stage interface is `--stage gpu-smoke --reserve 1.00 --require-zero-instances`, followed by `--stage metric-path --reserve 1.00 --require-zero-instances` only after smoke finalization. Both stages must produce a terminal result, integrity bundle, guard anchor receipt, and three-read absence proof.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Execute staged live GPU smoke and metric-path canary only through current passing gates</name>
  <files>evidence-root-anchors/gpu-smoke-<run-id>.json, evidence-root-anchors/metric-path-<run-id>.json, artifacts/runs/<run-id>/run-manifest.json, artifacts/runs/<run-id>/provider/, artifacts/runs/<run-id>/report/, artifacts/runs/<run-id>/kubernetes/, artifacts/runs/<run-id>/metrics/</files>
  <action>Apply D-01 through D-07. Before each stage, run the current read-only provider, guard, report-fixture, security, inventory, ledger, image-digest, and Phase 1 passing-verification checks. If any input fails, do not create: emit a signed, checksummed blocked limitation record naming the failed prerequisite. If gates pass, reserve at most $1.00 with Decimal, arm the independent guard, calculate immutable deadline and report-start time, issue exactly one create, and reconcile ambiguity only by current unique label. Run GPU/CUDA/KVM smoke first; only after its integrity/anchor/teardown/three-read absence finalization may metric-path run. For metric-path, capture the complete specified evidence path and one warm-up plus one measured request. Classify only attributable contract/GPU/CUDA/image/host defects as provider faults; report those before exact teardown, never report unresolved diagnosis. Commit only root-anchor metadata with `git commit -s` after guard receipt, never unreviewed mutable raw paid artifacts.</action>
  <verify><automated>python scripts/run_canary.py --stage gpu-smoke --reserve 1.00 --require-zero-instances &amp;&amp; python scripts/run_canary.py --stage metric-path --reserve 1.00 --require-zero-instances</automated></verify>
  <done>Either no paid request occurred and a precise blocked limitation bundle proves why, or both finalized stages have valid root anchors, required evidence/provenance, safe terminal states, and three separately timestamped absence confirmations.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Local controller to paid provider | A create can consume budget and return delayed/ambiguous state. |
| Rented VM/Kubernetes to captured evidence | GPU and metrics claims require actual observations, not container or synthetic approximations. |
| Fault classification to desktop report | False reporting risks account harm and time beyond deadline. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-18 | Elevation | paid dispatcher | mitigate | Fresh passing gates, Decimal reservation, one create, independent armed guard, and zero/same-run inventory. |
| T-02-19 | Tampering | provider lifecycle | mitigate | Exact offer/instance/nonce/label checks; journaled side-effect intent; delayed-create reconciliation only. |
| T-02-20 | Repudiation | real-GPU evidence | mitigate | Raw hop-level metrics, manifests, lifecycle/cost records, checksums, root anchor, guard receipt, and provenance. |
| T-02-21 | Denial of service | report/teardown | mitigate | Immutable report deadline, 60-second cap, break-glass guard, exact teardown, and three-read absence proof. |
</threat_model>

<success_criteria>Paid activity is impossible without a fresh Phase 1 pass and every live gate; any permitted stage yields either complete real-GPU evidence or a precise safe limitation with absence proof.</success_criteria>
<output>Create `.planning/phases/02-live-gpu-canary-and-paired-comparison/02-06-SUMMARY.md` when done.</output>
