---
phase: 02-live-gpu-canary-and-paired-comparison
plan: 08
type: execute
wave: 4
depends_on: [02-06, 02-07]
files_modified:
  - evidence-root-anchors/comparison-<run-id>.json
  - artifacts/runs/<run-id>/comparison/
  - artifacts/runs/<run-id>/provider/
  - artifacts/runs/<run-id>/metrics/
autonomous: true
requirements: [PAIR-01, PAIR-02, PAIR-03, PAIR-04]
user_setup:
  - service: Vast.ai
    why: "Comparison proceeds only after current read-only preflight proves two compatible ready capacities, with no pre-existing unowned instances."
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - No comparison create occurs unless validated canary, $3.00 remaining Decimal budget, and two current matched capacities pass.
    - Three complete blocks in A/B, B/A, A/B order produce real-GPU evidence with exact teardown and absence for every capacity.
    - Any unavailable capacity, broken gate, or incomplete block is precisely labeled blocked/exploratory and cannot support a paired claim.
  artifacts:
    - path: artifacts/runs/<run-id>/comparison/
      provides: Three-block trace/control/metric records or explicit exploratory/blocked result.
    - path: evidence-root-anchors/comparison-<run-id>.json
      provides: Guard-receipted root anchor for the finalized comparison lifecycle.
  key_links:
    - from: artifacts/runs/<run-id>/comparison/
      to: src/srecon26_poc/comparison.py
      via: Three-block control and classification contract
      pattern: AB,BA,AB|EXPLORATORY_ONLY|real-gpu
---

<objective>
Execute the conditional paired comparison only when its separate two-capacity, budget, canary, and safety gates all pass.

Purpose: Obtain three complete matched blocks or preserve a limitation/exploratory record without presenting unsupported performance results.
Output: Validated real-GPU three-block evidence and root anchor, or a precise no-create/partial exploratory record.
</objective>

<context>
@.planning/phases/02-live-gpu-canary-and-paired-comparison/02-06-SUMMARY.md
@.planning/phases/02-live-gpu-canary-and-paired-comparison/02-07-SUMMARY.md
@.planning/phases/02-live-gpu-canary-and-paired-comparison/02-CONTEXT.md
<interfaces>
Use `scripts/run_comparison.py --reserve 3.00 --blocks 3 --order AB,BA,AB`. It must consume final validated canary evidence, current two-capacity contracts, controller/guard/report/integrity interfaces, and comparison decision. It emits `VALID_REAL_GPU`, `EXPLORATORY_ONLY`, or `BLOCKED_NO_CREATE`; only the first may become a Phase 3 paired-chart input.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Execute the gated three-block paired comparison or preserve its limitation</name>
  <files>evidence-root-anchors/comparison-<run-id>.json, artifacts/runs/<run-id>/comparison/, artifacts/runs/<run-id>/provider/, artifacts/runs/<run-id>/metrics/</files>
  <action>Apply D-01, D-02, D-03, D-04, D-06, D-07, and D-08. Refresh all read-only provider, inventory, exact-offer, guard, report-fixture, security, Phase 1 pass, canary validation, and ledger checks. Require two compatible ready KVM capacities and at least $3.00 remaining before reserving/creating; otherwise write `BLOCKED_NO_CREATE` and do not spend. If allowed, run exactly A/B, B/A, A/B using one frozen control matrix and trace identity, capture all specified metrics, report attributable provider faults before teardown, and prove absence for every capacity after each lifecycle. Missing/failed blocks produce `EXPLORATORY_ONLY`, retain raw operational evidence, and prohibit comparison conclusion/claim/chart output. Anchor only finalized root metadata after guard receipt with `git commit -s`.</action>
  <verify><automated>python scripts/run_comparison.py --reserve 3.00 --blocks 3 --order AB,BA,AB</automated></verify>
  <done>Either no provider mutation occurred and a blocked record identifies the failing gate, or exactly three complete matched real-GPU blocks have anchored artifacts, exact teardown, and three-read absence proof for each capacity; partial results are visibly exploratory-only.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Current capacity contracts to two paid creates | Capacity drift/partial allocation can waste budget or break comparability. |
| Collected blocks to conclusion consumers | A partial, mixed-provenance, or mismatched run can otherwise become an invalid claim. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-25 | Elevation | comparison dispatch | mitigate | Fresh full gates, minimum remaining Decimal budget, exact current capacity contracts, one-create/reconcile lifecycle, independent guard. |
| T-02-26 | Tampering | run blocks | mitigate | Freeze and record all controls/trace, check provenance and completeness, reject mismatch. |
| T-02-27 | Repudiation | comparison finalization | mitigate | Per-capacity lifecycle/cost/absence records, checksums, guard receipts, and root anchor. |
| T-02-28 | Denial of service | dual teardown | mitigate | Per-instance immutable deadline and guard exact teardown; lack of one capacity produces no claimed comparison. |
</threat_model>

<success_criteria>Only a fully gated, three-block, real-GPU, rooted, absence-proved run can satisfy comparison requirements; all other outcomes are no-create or exploratory without a claim.</success_criteria>
<output>Create `.planning/phases/02-live-gpu-canary-and-paired-comparison/02-08-SUMMARY.md` when done.</output>
