---
phase: 02-live-gpu-canary-and-paired-comparison
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/srecon26_poc/vast_provider.py
  - scripts/preflight_vast.py
  - providers/vast/fixtures/account.json
  - providers/vast/fixtures/offers.json
  - providers/vast/fixtures/instances.json
  - tests/unit/test_vast_provider.py
  - tests/integration/test_vast_delayed_create.py
autonomous: true
requirements: [GPU-01, GPU-07]
must_haves:
  truths:
    - Provider account, inventory, and offer inspection is read-only, bounded, redacted, and repeatable.
    - An ambiguous create is reconciled by the current unique label and never retried.
    - A paid run can freeze an exact current offer contract without treating a historical offer as valid.
  artifacts:
    - path: src/srecon26_poc/vast_provider.py
      provides: Bounded Vast CLI provider adapter and normalized redacted records.
    - path: scripts/preflight_vast.py
      provides: Read-only current account, inventory, autobill, and offer-capability snapshot.
  key_links:
    - from: scripts/preflight_vast.py
      to: src/srecon26_poc/vast_provider.py
      via: Read-only account, inventory, and offer queries
      pattern: account_snapshot|list_instances|get_offer
---

<objective>
Implement the test-first, read-only-first Vast CLI adapter used to evaluate current readiness without creating, destroying, or reporting any resource.

Purpose: Paid execution must rely on a current exact contract, not historical marketplace observations.
Output: Bounded adapter, redacted fixtures, delayed-create reconciliation tests, and a read-only preflight command.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safety-foundation-and-local-evidence/01-VERIFICATION.md
@.planning/phases/02-live-gpu-canary-and-paired-comparison/02-CONTEXT.md
<interfaces>
Consume `Provider`, `AccountSnapshot`, `OfferContract`, `InstanceContract`, `RunJournal`, and `ExposureLedger`. Export `VastCliProvider` with argument-array subprocess calls, bounded read-only methods, `create_once`, label reconciliation, and `destroy_exact`; no method may serialize a credential.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the bounded read-only Vast provider adapter</name>
  <files>src/srecon26_poc/vast_provider.py, scripts/preflight_vast.py, providers/vast/fixtures/account.json, providers/vast/fixtures/offers.json, providers/vast/fixtures/instances.json, tests/unit/test_vast_provider.py, tests/integration/test_vast_delayed_create.py</files>
  <behavior>
    - Fixture account output reports autobill state and never includes a provider secret.
    - CLI parsing normalizes current offer, instance, and account records into existing contracts without shell interpolation.
    - A timeout after one create is reconciled by exactly one fresh nonce-bound label; zero/multiple matches halt safely and do not retry.
    - `--read-only` records redacted account, zero-or-owned inventory, and capability summary without emitting create, destroy, or report commands.
  </behavior>
  <action>Implement per D-02 and D-06. Start from failing fixture tests. Use subprocess argument lists, command timeouts, explicit JSON parsing, and redacted normalized output. Keep every current offer field an input to preflight/future contract freeze; do not embed offer IDs, prices, hosts, template IDs, capacity, or historical inventory in code defaults. Include tests that reject ambiguous or multiple-label ownership.</action>
  <verify><automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests/unit/test_vast_provider.py tests/integration/test_vast_delayed_create.py</automated></verify>
  <done>Read-only fixtures and delayed-create tests pass; the preflight contract can produce a redacted current snapshot without any provider mutation.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Controller to Vast CLI | CLI output, timeouts, and account state are untrusted and time-sensitive. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Spoofing | vast_provider.py | mitigate | Normalize and match exact nonce-bound labels plus instance contract fields. |
| T-02-02 | Information disclosure | Vast CLI records | mitigate | Redact credential-bearing fields; use argument arrays and never log secrets. |
| T-02-03 | Denial of service | create reconciliation | mitigate | Apply per-command timeout; halt on zero/multiple current-label matches; never retry. |
| T-02-SC | Tampering | package setup | accept | No package is installed; all behavior is standard-library and fixture tested. |
</threat_model>

<success_criteria>All adapter tests pass without network access or provider mutation, and its read-only preflight records only current redacted facts.</success_criteria>
<output>Create `.planning/phases/02-live-gpu-canary-and-paired-comparison/02-01-SUMMARY.md` when done.</output>
