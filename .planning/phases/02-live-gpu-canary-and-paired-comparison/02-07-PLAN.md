---
phase: 02-live-gpu-canary-and-paired-comparison
plan: 07
type: tdd
wave: 2
depends_on: [02-04]
files_modified:
  - src/srecon26_poc/comparison.py
  - scripts/run_comparison.py
  - tests/unit/test_comparison.py
  - tests/integration/test_comparison_runner.py
  - docs/runbooks/paired-comparison.md
autonomous: true
requirements: [PAIR-01, PAIR-02, PAIR-03, PAIR-04]
must_haves:
  truths:
    - A comparison plan is allowed only with two compatible ready GPU capacities, $3.00 remaining budget, a validated canary, and matched controls.
    - Fewer than three complete blocks produce exploratory evidence only and cannot emit comparative claim data.
  artifacts:
    - path: src/srecon26_poc/comparison.py
      provides: Comparison-plan validation and exploratory-only classification.
    - path: scripts/run_comparison.py
      provides: Fixture-tested paired runner with no default paid dispatch.
    - path: docs/runbooks/paired-comparison.md
      provides: Required control matrix and evidence contract.
  key_links:
    - from: scripts/run_comparison.py
      to: src/srecon26_poc/comparison.py
      via: Validation before any live dispatch
      pattern: validate_comparison_plan|EXPLORATORY
---

<objective>
Build and prove the conditional paired-comparison validator and fixture runner without acquiring capacity or starting workloads.

Purpose: Prevent accidental comparison claims from mismatched or incomplete data.
Output: Comparator, offline runner, tests, and matched-controls runbook.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-live-gpu-canary-and-paired-comparison/02-CONTEXT.md
<interfaces>
Export `ComparisonPlan`, `ComparisonDecision`, block/result types, `validate_comparison_plan(plan, remaining)`, and `analyze_comparison(result)`. A plan includes current validated-canary reference, two compatible capacities, pinned model revision/image digest/GPU class, trace hash, token schedule, seed, warm-up, window, replica limits, readiness policy, and fixed order `AB,BA,AB`.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement comparison eligibility and exploratory-only classification</name>
  <files>src/srecon26_poc/comparison.py, scripts/run_comparison.py, tests/unit/test_comparison.py, tests/integration/test_comparison_runner.py, docs/runbooks/paired-comparison.md</files>
  <behavior>
    - Missing validated canary, second compatible ready capacity, $3.00 Decimal headroom, or any control mismatch blocks dispatch.
    - Changing model revision, image digest, GPU class, trace hash, prompt/token schedule, seed, warm-up, window, replica/readiness control, or order invalidates comparability.
    - Results with fewer than three complete paired blocks classify `EXPLORATORY_ONLY`, reject comparative conclusions, and preserve raw operational evidence.
    - Fixture runner records queue, TTFT, TPOT, end-to-end latency, GPU/KV/CPU, desired/ready replicas, readiness delay, failures, trace identity, and real-GPU provenance requirement.
  </behavior>
  <action>Implement per D-06 and D-08. Begin with failing mismatched-control, budget, incomplete-block, and provenance tests. Parse all amounts with Decimal and keep runner fixture mode unable to invoke a provider. Do not substitute historical offer/capacity findings for current two-capacity readiness, and do not generate charts or a performance claim; those remain Phase 3 consumers of validated results.</action>
  <verify><automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests/unit/test_comparison.py tests/integration/test_comparison_runner.py</automated></verify>
  <done>Fixture tests reject every eligibility/control violation and establish that partial comparisons are exploratory-only, not a result claim.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Current capacity/preflight to comparison dispatch | Marketplace availability can drift or differ between capacity observations. |
| Paired-run configuration to result claims | Changed controls or partial blocks can generate invalid comparisons. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-22 | Spoofing | comparison eligibility | mitigate | Current two-capacity contract and validated-canary reference are required, never historical examples. |
| T-02-23 | Tampering | comparison controls | mitigate | Immutable control matrix and trace hash validated before every block. |
| T-02-24 | Repudiation | comparison result | mitigate | Preserve block-level records, provenance, trace identity, and explicit exploratory classification. |
</threat_model>

<success_criteria>Offline comparison tests prove no partial or mismatched experiment can be represented as a valid paired comparison.</success_criteria>
<output>Create `.planning/phases/02-live-gpu-canary-and-paired-comparison/02-07-SUMMARY.md` when done.</output>
