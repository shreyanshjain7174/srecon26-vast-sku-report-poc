---
phase: 02-live-gpu-canary-and-paired-comparison
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - infra/k3s/bootstrap.sh
  - infra/k3s/nvidia-runtime.toml
  - infra/k3s/namespace.yaml
  - infra/k3s/device-plugin.yaml
  - infra/k3s/vllm.yaml
  - infra/k3s/prometheus.yaml
  - infra/k3s/metrics-server.yaml
  - infra/k3s/prometheus-adapter.yaml
  - infra/k3s/hpa-observer.yaml
  - src/srecon26_poc/canary.py
  - tests/unit/test_canary.py
  - tests/integration/test_k3s_manifests.py
autonomous: true
requirements: [GPU-01, GPU-03, GPU-04, GPU-05, GPU-06]
must_haves:
  truths:
    - A paid canary stops before bootstrap if its current KVM host facts or pinned image digest checks are incomplete.
    - Manifests describe the full vLLM-to-Prometheus-to-custom-metrics-to-HPA path using `autoscaling/v2`.
    - A readiness verdict requires allocated GPU, healthy metric sources, and fresh bounded samples before workload begins.
  artifacts:
    - path: src/srecon26_poc/canary.py
      provides: KVM capability and canary-readiness decisions.
    - path: infra/k3s/vllm.yaml
      provides: Pinned-digest vLLM pod requesting exactly one GPU.
    - path: infra/k3s/prometheus-adapter.yaml
      provides: Queue/KV custom-metrics API mapping.
    - path: infra/k3s/hpa-observer.yaml
      provides: autoscaling/v2 HPA observation object.
  key_links:
    - from: infra/k3s/vllm.yaml
      to: infra/k3s/device-plugin.yaml
      via: nvidia.com/gpu resource request and allocatable GPU readiness
      pattern: nvidia\.com/gpu
    - from: infra/k3s/prometheus-adapter.yaml
      to: infra/k3s/hpa-observer.yaml
      via: named queue and KV custom metrics
      pattern: queue|kv|autoscaling/v2
---

<objective>
Define and test the full-KVM capability gate and single-node k3s metric-path manifests offline.

Purpose: A native container or unverified KVM template cannot substitute for the required Kubernetes observation path.
Output: Capability/readiness evaluator, pinned manifests, and schema/fixture tests.
</objective>

<context>
@docs/superpowers/specs/2026-09-23-srecon26-llm-hpa-poc-design.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-live-gpu-canary-and-paired-comparison/02-CONTEXT.md
<interfaces>
Export `KvmFacts`, `CapabilityDecision`, `CanarySnapshot`, `ReadinessDecision`, `evaluate_kvm_capabilities`, and `evaluate_canary_readiness`. The runtime receives the image digest/model revision from the current frozen run contract; do not invent or bake in an offer/template/host.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define the gated KVM k3s GPU metric path</name>
  <files>infra/k3s/bootstrap.sh, infra/k3s/nvidia-runtime.toml, infra/k3s/namespace.yaml, infra/k3s/device-plugin.yaml, infra/k3s/vllm.yaml, infra/k3s/prometheus.yaml, infra/k3s/metrics-server.yaml, infra/k3s/prometheus-adapter.yaml, infra/k3s/hpa-observer.yaml, src/srecon26_poc/canary.py, tests/unit/test_canary.py, tests/integration/test_k3s_manifests.py</files>
  <behavior>
    - Capability evaluation rejects missing systemd, cgroup v2, privilege, containerd/NVIDIA runtime, `nvidia-smi`, CUDA, or current image-digest resolution.
    - Readiness rejects absent node allocatable GPU, unready device plugin/vLLM pod, unhealthy Prometheus target, or absent/stale CPU/queue/KV metric values.
    - Manifests request one GPU for vLLM, keep Prometheus/vLLM/Kubernetes API private behind SSH tunnel, expose queue/KV custom metrics, and use `autoscaling/v2`.
  </behavior>
  <action>Implement per D-02, D-05, and D-06. Begin with failing host-fact/readiness tests and offline manifest contract tests. Require current digest revalidation before an allowed decision; use the approved digest only as a declared contract input, never a tag pull. Do not bootstrap a remote machine in this plan. Include warm-up versus measured request readiness so container-running cannot be mistaken for workload-ready.</action>
  <verify><automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests/unit/test_canary.py tests/integration/test_k3s_manifests.py &amp;&amp; kubectl apply --dry-run=client -f infra/k3s/ &amp;&amp; semgrep --config .semgrep.yml --error infra/k3s scripts src tests</automated></verify>
  <done>Capability and readiness tests pass, manifests client-validate, and no remote KVM, provider, or cluster is created.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Current KVM host to canary bootstrap | A marketplace host may not provide the advertised privileged GPU/Kubernetes prerequisites. |
| Kubernetes metrics path to HPA | Adapter/API/RBAC defects can create a false observation-path claim. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-11 | Spoofing | KvmFacts | mitigate | Require current probes for each KVM/GPU capability and current digest resolution. |
| T-02-12 | Tampering | infra/k3s manifests | mitigate | Fixture tests, client-side validation, immutable digest contract, and Semgrep scan. |
| T-02-13 | Information disclosure | k3s networking | mitigate | Restrict public exposure to controller-source SSH; use tunnel/localhost for service APIs. |
</threat_model>

<success_criteria>Offline tests prove every missing KVM or observation-layer prerequisite blocks the canary, and all manifests validate without remote mutation.</success_criteria>
<output>Create `.planning/phases/02-live-gpu-canary-and-paired-comparison/02-04-SUMMARY.md` when done.</output>
