---
phase: 02-live-gpu-canary-and-paired-comparison
plan: 05
type: tdd
wave: 2
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - scripts/run_canary.py
  - tests/integration/test_canary_runner.py
  - docs/runbooks/live-canary.md
autonomous: true
requirements: [GPU-01, GPU-02, GPU-03, GPU-04, GPU-05, GPU-06, GPU-07]
must_haves:
  truths:
    - Offline runner simulation proves every success/fault/timeout/ambiguity path stays within the immutable deadline and ends with exact teardown plus absence proof.
    - The runner does not expose a paid execution branch until it can validate the complete current gate evidence.
  artifacts:
    - path: scripts/run_canary.py
      provides: Bounded smoke and metric-path lifecycle runner with safe terminal/blocked results.
    - path: tests/integration/test_canary_runner.py
      provides: Paid-path simulation coverage without provider mutation.
    - path: docs/runbooks/live-canary.md
      provides: Run artifact schema, required gate evidence, and safe terminal meanings.
  key_links:
    - from: scripts/run_canary.py
      to: src/srecon26_poc/controller.py
      via: Journaled create/reconcile, report, destroy, and absence lifecycle
      pattern: ExperimentController|create_or_reconcile|prove_absent
    - from: scripts/run_canary.py
      to: src/srecon26_poc/canary.py
      via: Capability and readiness decisions before bootstrap/workload
      pattern: evaluate_kvm_capabilities|evaluate_canary_readiness
---

<objective>
Implement and test the bounded canary orchestrator entirely with fixtures before its separately planned paid stages may execute.

Purpose: The paid stage must be a tested consumer of current gates, never the place where lifecycle logic is first exercised.
Output: Offline-safe runner, exhaustive fixture suite, and runbook.
</objective>

<context>
@.planning/phases/01-safety-foundation-and-local-evidence/01-VERIFICATION.md
@.planning/phases/02-live-gpu-canary-and-paired-comparison/02-01-SUMMARY.md
@.planning/phases/02-live-gpu-canary-and-paired-comparison/02-02-SUMMARY.md
@.planning/phases/02-live-gpu-canary-and-paired-comparison/02-03-SUMMARY.md
@.planning/phases/02-live-gpu-canary-and-paired-comparison/02-04-SUMMARY.md
@.planning/phases/02-live-gpu-canary-and-paired-comparison/02-CONTEXT.md
<interfaces>
Consume `VastCliProvider`, concrete `Guard`, `ReportGate`, `ExperimentController`, `ExposureLedger`, `evaluate_kvm_capabilities`, `evaluate_canary_readiness`, and integrity bundle APIs. Emit one declared `artifacts/runs/<run-id>/` manifest whose provenance is `real-gpu` only after live evidence is captured; blocked/fixture results use non-GPU provenance and cannot be promoted.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the bounded smoke and metric-path runner in offline simulation</name>
  <files>scripts/run_canary.py, tests/integration/test_canary_runner.py, docs/runbooks/live-canary.md</files>
  <behavior>
    - Simulation blocks before provider create when Phase 1 verification is not passed, guard/report/offer/inventory/ledger/security inputs are absent, or a safety breach exists.
    - KVM capability failure produces a precise limitation bundle, exact teardown if an instance exists, and three absence reads without k3s bootstrap.
    - Smoke records contract, GPU/CUDA/KVM facts, cost timestamps, diagnosis, report handling, teardown, and absence.
    - Metric-path starts only after finalized smoke; it requires current node GPU/device plugin, vLLM warm-up and measured request, Prometheus, resource/custom metrics APIs, HPA, events, and timing fields.
    - Fixtures cover success, confirmed SKU fault, unresolved diagnosis, report timeout, create ambiguity, and deadline teardown.
  </behavior>
  <action>Implement per D-01, D-02, D-04, D-05, D-06, and D-07. Start with failing simulation tests. Calculate and journal hard deadline plus immutable `report_start_by` before create. Make gate evaluation explicit and deterministic: fresh Phase 1 `status: passed`; current Semgrep/no-spend result; live independent-guard attestation; verified report fixture; read-only exact offer contract; empty/same-run-owned inventory; Decimal headroom/reservation; and no safety breach. When any condition fails, emit a blocked limitation record and return before any create. Keep paid dispatch behind an explicit stage implementation to be consumed only by Plan 02-06.</action>
  <verify><automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests/integration/test_canary_runner.py &amp;&amp; semgrep --config .semgrep.yml --error scripts src tests</automated></verify>
  <done>Fixture tests demonstrate every terminal path remains bounded and no fixture or missing-gate path can create a provider resource.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Gate evidence to paid dispatcher | Stale/incomplete evidence could otherwise permit unintended spend. |
| Runner to provider/guard/report/Kubernetes | Multiple external side effects must preserve exact ordering and deadline limits. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-14 | Elevation | run_canary.py | mitigate | Fail closed on every named gate and require fresh Phase 1 passed verification. |
| T-02-15 | Repudiation | run manifest/journal | mitigate | Capture gate inputs, contract, lifecycle timestamps, diagnosis, receipt, cost, and absence proof. |
| T-02-16 | Denial of service | report path | mitigate | Fixed deadline/report-start calculation and guard deadline takeover in all timeout fixtures. |
| T-02-17 | Tampering | provenance | mitigate | Blocked/fixture outputs are explicitly non-GPU and cannot satisfy GPU claim fields. |
</threat_model>

<success_criteria>Offline canary simulation proves a complete gate set is necessary and all failure cases reach a safe, evidence-bearing terminal state without provider mutation.</success_criteria>
<output>Create `.planning/phases/02-live-gpu-canary-and-paired-comparison/02-05-SUMMARY.md` when done.</output>
